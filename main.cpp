#include <iostream>
#include "include/Graph.h"

// Prototypes
Path dijkstra(const Graph& g, uint source, uint destination);
Path aStar(const Graph& g, uint source, uint destination);

int main() {
    return 0;
}